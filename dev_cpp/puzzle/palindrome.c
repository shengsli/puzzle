#include <stdio.h>

void print_substr(char *str, int lo, int hi)
{
	int i;
	for (i = lo; i <= hi; ++i)
		printf("%c", str[i]);
}

int longest_palstr(chat *str)
{
	int len = strlen(str);
	int i;
	for (i = 1; i < start
}

int main(int argc, char *argv[])
{
	char str[] = "palindromemordstring";
	print_substr(str, 1,5);
    return 0;
}
